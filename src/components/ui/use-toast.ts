import { useToast } from "@/hooks/use-toast";

export const toast = {
  success: (msg: string) => {
    // implementação
  },
  error: (msg: string) => {
    // implementação
  },
  // outros métodos...
};

export { useToast };
